# user-manager
User management system built with .NET using Clean Architecture. Provides cookie-based authentication, role-based authorization, and CRUD operations for users and roles. Features DTO validation, AutoMapper profiles, and an EF Core infrastructure layer. Designed for modularity, scalability, and clear separation of concerns.
